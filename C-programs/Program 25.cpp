/*program 25
22.5.25
check whethere the given number is negative or not
*/
#include<stdio.h>
main()
{
	int a;
	scanf("%d", &a);
if (a < 0)
    printf("%d is negative", a);
else
    printf("%d is not negative", a);
}
