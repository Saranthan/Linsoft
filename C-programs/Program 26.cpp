/*
program 26
22.5.25
check whethere the given number is zero or not
*/
#include<stdio.h>
main()
{
	int a;
	scanf("%d",  &a);
if ( a == 0)
    printf("%d is zero", a);
else
    printf("%d is not zero", a);
}
