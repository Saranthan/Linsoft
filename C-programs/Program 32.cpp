/*
program 32
25.5.25
check whethere the number is divisible or not
*/
#include<stdio.h>
main()
{
	int a;
	scanf("%d", &a);
if (a %5 == 0)	
    printf("It is divisible by 5", a);
else
    printf("It is not divisible by 5", a);
}
